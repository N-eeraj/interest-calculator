export enum SchemeType {
  FD = "fd",
  RD = "rd",
  MIS = "mis",
};

export enum InvestmentType {
  LUMP_SUM = "lump-sum",
  RECURRING = "recurring",
};

export enum CompoundingType {
  NONE = "nil",
  QUARTERLY = "qtr",
  YEARLY = "yr",
};
